dist: xenial
language: python
python:
  - "3.7"
services:
  - redis-server
install:
  - pip install -r requirements.txt
script:
  - flake8 service_api/
  - mypy service_api/ --ignore-missing-imports
  - py.test tests/